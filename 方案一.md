这是一份针对 **AI 通用决策推演引擎** 的深度重构方案。该方案核心在于消除“业务硬编码”，建立基于**向量计算**的动态评价体系，并将决策权从静态数据移交给“参数+算法”。

---

# 🧠 通用决策推演引擎重构方案 (V3.0)

## 一、 核心架构：去业务化逻辑链

组件不再理解具体业务，仅处理**维度 (Dimensions)**、**权重 (Weights)** 与 **敏感度 (Sensitivities)**。

### 1. 数据模型重构 (The Source of Truth)

废弃 `scores` 独立字段，将 `treeData` 作为唯一真值来源。

- **维度定义 (Dimensions)**：定义决策的评价标准（如：收益、风险、难度）。
- **敏感度 (Sensitivity)**：方案对特定维度的响应系数（-1 到 1）。
- **累积概率 (Cumulative Probability)**：路径各步局部概率的连乘结果。

---

## 二、 数学推演模型：动态评分逻辑

### 1. 动态评分公式

每个节点（Node）的实时分值 $V_{display}$ 计算如下：
$$V_{display} = V_{base} + \sum_{i=1}^{n} \left( (W_{i} - 0.5) \times S_{i} \times K \right)$$

- $V_{base}$: LLM 给出的中立基准分。
- $W_{i}$: 用户滑块权重 (0~1)。
- $S_{i}$: 节点对维度 $i$ 的敏感度。
- $K$: 放大系数（建议为 100），用于拉开分值差距。

### 2. 概率推算逻辑

消除 `P = value/100` 的错误映射。

- **局部概率 ($P_{local}$)**：同级兄弟节点之间的竞争概率，由 LLM 显式生成。
- **累积概率 ($P_{cum}$)**：从根节点到当前节点的路径概率积。
  $$P_{cum} = \prod_{depth=0}^{N} P_{local}$$
- **UI 映射**：$P_{cum}$ 决定连线的**粗细**与**透明度**，而非节点数值。

---

---

## 三、 关键技术实现路径

### Step 1: 适配器层 (useDecisionModel.js)

在拿到 LLM 数据后立即执行 `enrichTree()`：

1.  **分配 ID**：为每个节点生成唯一 `id`。
2.  **概率预排**：递归计算 `cumulative_probability`。
3.  **敏感度对齐**：确保 `sensitivities` 的 Key 与 `dimensions` 的 ID 严格对应。

### Step 2: 重新模拟 (Re-simulation) 链路

点击按钮触发：

1.  读取当前滑块 `Ref` 数组。
2.  调用 `recalculateScores(weights)`：仅在前端循环 Tree 节点，更新 `displayValue`。
3.  触发 D3 `Update` 模式，刷新树形颜色和排序。

---

## 五、 LLM Prompt 契约调整 (推荐)

要求 LLM 必须按以下格式输出 `treeData` 以配合去业务化逻辑：

```markdown
1. 必须提供 dimensions 数组，包含所有影响决策的维度。
2. 方案层节点必须包含 sensitivities 对象，键名为 dimensions 的 id。
3. 每个节点必须包含 local_probability (0-1)，确保同级节点和为 1。
4. 移除全局 scores 字段，所有评分逻辑下放到 treeData 内部。
```

---

## 六、 总结：方案价值

1.  **逻辑自洽**：解决了“滑块调了但排序没变”的逻辑 Bug。
2.  **深度通用**：只需更换 Prompt 中的场景描述，无需修改一行前端代码即可适配职场、投资、教育等决策场景。
3.  **交互丝滑**：将推演压力放在前端向量计算，实现无刷新的毫秒级参数响应。
