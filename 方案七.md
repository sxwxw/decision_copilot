<!--
 * @Author: wxw
 * @Date: 2026-04-25 00:56:43
 * @LastEditors: wxw
 * @LastEditTime: 2026-04-25 00:56:43
 * @FilePath: \decision_copilot\方案七.md
-->

关于这两个工程细节，我的建议是：**保持算法的“直觉性”，但要严守代码的“解耦性”**。

### 1. `_pathRef` 的匹配策略：简单包含 + 语义优于算法

在处理 LLM 数据时，**复杂的算法（如 Levenshtein 距离）往往不如简单的语义规则有效**。因为 LLM 的“放飞自我”通常是加形容词或换同义词，而不是拼写错误。

我建议采用 **“包含关系 + 词干提取”** 的降级策略：

- **第一级：全量相等（Strict Equality）**
  - `node.name === event.name`
- **第二级：双向包含（Heuristic Containment）**
  - `node.name.includes(event.name)` OR `event.name.includes(node.name)`。
  - _理由_：LLM 经常在树节点写“市场突围”，在路径里写“通过市场突围”。包含关系能解决 80% 的问题。
- **第三级：位置索引回退（Positional Fallback）**
  - 如果同级子节点中没有名字能对上，直接按 `index` 匹配。
  - _逻辑_：如果 Path 的第一步是“Option A”下的第 1 个事件，那就强行关联到 Tree 中“Option A”下的第 1 个子节点。
  - **千万不要写编辑距离**：因为在节点名很短（4-6字）的情况下，编辑距离的误差极大。比如“市场萎缩”和“市场扩张”，编辑距离很小，但语义完全相反。

---

### 2. 清洗器的位置：独立 `sanitizeModel` 函数

**结论：坚决放在 `useDecisionModel.js` 的独立函数中，而不是 `adaptTree` 内部。**

#### 理由如下：

1.  **单一职责原则 (SRP)**：
    - `sanitizeModel` 的职责是**“数据治理”**（修正 LLM 的错误，对齐协议）。
    - `adaptTree` 的职责是**“视图转换”**（增加 D3 需要的布局字段、生成唯一 ID）。
    - 如果合在一起，当你需要调试“为什么分数没变”时，你分不清是 LLM 给错了，还是清洗算错了，还是转换出 Bug 了。

2.  **深度模拟的复用需求**：
    在深度模拟时，你可能需要对比新旧两份 JSON。如果你有一个独立的 `sanitizeModel`，你可以先对两份数据进行“标准化”处理，再进行 Diff 逻辑。

3.  **单元测试**：
    数据清洗逻辑是极易产生边界 Case 的地方。独立的函数意味着你可以脱离 UI 组件，直接写一个简单的脚本，喂入各种垃圾 JSON 来测试清洗器的鲁棒性。

---

### 3. 工程落地结构建议

建议的调用链路如下：

```javascript
// useDecisionModel.js

const runSimulation = async input => {
  const rawData = await api.fetchSimulation(input)

  // 1. 数据治理：把 LLM 的“幻觉”修正为“协议数据”
  const sanitizedData = sanitizeModel(rawData)

  // 2. 状态映射：将标准化后的数据存入 store
  // 这里的 adaptTree 只负责视图层需要的 id、x/y 坐标等
  state.model = adaptTree(sanitizedData)
}

/**
 * sanitizeModel: 逻辑对齐器
 */
function sanitizeModel(data) {
  // 按照我们讨论的 5 条清单进行清洗
  // 重点构建 _pathRef
  // ...
  return data
}
```

### 💡 一个关于“位置回退”的小贴士

在执行“位置回退”时，务必在控制台打印一个 `console.warn`。

> `[Sanitizer] 无法通过名称匹配路径节点 "突围成功"，已回退至位置匹配：索引 [0]`

这能让你在调试时一眼看出：是 LLM 名字起得太离谱，还是你的匹配逻辑出了断层。

**你现在的 `adaptTree` 里是不是已经塞了不少逻辑了？** 建议趁着这次“深度模拟”的重构，把它们彻底拆出来，这样你的 `DecisionTree.vue` 会变得非常清爽，只负责根据 `_pathRef` 去做视觉高亮。
