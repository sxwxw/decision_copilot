<!--
 * @Author: wxw
 * @Date: 2026-04-24 09:59:21
 * @LastEditors: wxw
 * @LastEditTime: 2026-04-24 10:22:26
 * @FilePath: \decision_copilot\方案四.md
-->

这三个问题直击**决策引擎内核**的稳定性与合理性。尤其是关于“死数据”和“概率连乘”的思考，决定了系统是看起来像个玩具，还是一个严谨的辅助工具。

以下是针对这三个问题的深度拆解及优化方案：

---

### 1. 决策树与分叉对比的“死数据”问题

**核心结论：它们必须是“活”的，否则会产生认知断层。**

如果用户在左侧调整了权重，右侧列表排序变了，但中间的决策树纹丝不动，用户会怀疑数据的真实性。

- **视觉响应建议**：
  - **高亮受影响分支**：在 `DecisionTree.vue` 中，如果某个节点的 `trade_offs` 包含当前正在调整的参数，给该节点增加一个微弱的“呼吸感”边框（使用你定义的品牌蓝 `--el-color-primary`）。
  - **数值同步**：节点旁的 `P=XX%` 必须实时更新。由于我们已经有了 `cumulativeProb` 的计算逻辑，滑块触发 `recalc` 时，树应该利用 D3 的 `transition` 实现数字滚动。
  - **ForkComparison 的价值**：分叉对比的意义就在于“在不同参数下看选择”。如果它是死的，那它就失去了**“反事实推演”**的意义。建议在对比面板中增加一个“敏感度标识”，标注哪些变量是导致这两条路径产生分歧的关键。

---

### 2. 概率调整的“乘法 vs 加法”逻辑

**核心结论：乘法在数学上更严谨，但在“感知”上需要保护。**

你观察到的“绝对变化量不同”正是**条件概率**的特性。高概率路径通常是“主干道”，主干道的波动对最终结果的影响权重确实应该更大。

- **业务逻辑合理性**：
  - **合理性**：合理。例如“产品落地”概率从 80% 降到 60%（跌幅 25%），比一个本身只有 5% 概率的“中彩票”事件波动到 4% 要致命得多。
  - **潜在风险**：如果一直连乘 0.85，低概率路径会迅速衰减至接近 0，导致“长尾可能性”彻底消失。
- **优化建议（归一化保护）**：
  在同一 Option 下的所有路径调整完概率后，必须进行**重新归一化（Normalizing）**：
  $$P'_{i} = \frac{P_{i} \times Modifier_{i}}{\sum (P_{j} \times Modifier_{j})}$$
  这样可以确保无论参数怎么变，该选项下的所有路径概率之和永远为 1，避免概率溢出或凭空消失。

---

### 3. 滑块防抖与计算压力

**核心结论：必须增加防抖（Debounce）或节流（Throttle），不仅是为了性能，更是为了“视觉优雅”。**

虽然目前数据量小，但频繁触发 `recalcScores()` 会导致：

1.  **D3 渲染冲突**：D3 的 transition 动画如果被频繁中断并重启，会出现“卡顿”或“抖动”感。
2.  **UI 噪音**：数字跳动过快会导致视觉疲劳。

- **实施方案**：
  - **输入节流 (Throttling)**：在拖动过程中，限制每 16ms（即 60fps）触发一次计算，确保动画流畅。
  - **结果防抖 (Debouncing)**：在用户松开滑块后的 200ms，触发一次深度的“归因文案”生成。

---

---

### 🎨 极致简约的视觉反馈建议 (针对问题 1)

既然不想要“AI味”，当滑块拖动时，不要用闪烁效果。

- **做法**：在决策树的连线上，根据概率的变化，让线宽（stroke-width）发生平滑的粗细变化。
- **感知**：用户会感觉到随着他拉动“风险偏好”，某些路径变得“宽阔平坦”（概率变大），而某些路径变得“细如羊肠”（概率变小）。这种物理感比文字提示要高级得多。
