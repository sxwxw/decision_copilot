<!--
 * @Author: wxw
 * @Date: 2026-04-24 22:38:23
 * @LastEditors: wxw
 * @LastEditTime: 2026-04-24 22:38:23
 * @FilePath: \decision_copilot\方案五.md
-->

这是一个极其深刻的架构抉择。你触及了决策分析中的核心矛盾：**“参数化模型” vs. “情境化叙事”**。

在专业决策领域，这被称为**“结构性灵敏度（Structural Sensitivity）”**。如果只动分数，那是量变；如果树的结构变了，那是质变。

针对你的五个设计抉择，我给出一套能够兼顾“逻辑专业性”与“去 AI 味简洁感”的深度方案：

---

### 1. 触发时机：方案 A+（显式异步触发）

**结论：必须是按钮触发，但要配合“逻辑失效预警”。**
不要用方案 B（自动触发），因为这会破坏用户的“心流”，且 LLM 的生成不可控。

- **交互策略**：当用户拖动滑块导致参数偏移过大（如单项偏离 > 40 或累计偏离 > 80）时，在“深度模拟”按钮上显示一个微小的红点或动效。
- **话术提示**：按钮下方出现一行极细的灰色文字：_“当前参数已显著偏离初始假设，建议重构逻辑链路。”_
- **价值**：这给了用户**掌控感**。用户知道当前的数学计算是“线性推演”，而点下按钮是“专家重审”。

---

### 2. 输入契约：发送 `currentModel`，但限制 `options`

**结论：必须发送上下文，且实行“选项锚定”。**
为了解决你担心的“React 变成 Angular”的问题，Prompt 必须包含**结构锚点**：

- **输入**：
  ```json
  {
    "userInput": "...",
    "paramValues": { "开发效率": 95, ... },
    "context": {
      "fixedOptions": ["React", "Vue", "jQuery"], // 强制锁定第一层
      "previousLogic": "..." // 让 LLM 参考之前的路径，避免逻辑打架
    }
  }
  ```
- **逻辑**：告诉 LLM：“主选项已确定，请基于最新的偏好参数，重新推演每个选项背后的因果演化路径。”

---

### 3. 前端接收：`runSimulation` 是一次“无损升级”

**结论：这确实是 `buildModel`，但需要“视觉平滑”。**
为了避免“选中状态丢失”的突兀感：

- **Id 持久化**：如果选项名称没变，前端在 `adaptTree` 时应保留第一层节点的 `id`。
- **差异化渲染**：
  - 保留用户当前的滑块位置（不要重置）。
  - 使用 D3 的 **“交叉淡入淡出（Cross-fade）”**：旧树变淡，新树在原位置长出来。

---

### 4. 用户体验路径：从“计算”到“论证”

你的路径设计非常清晰，建议在 UI 上通过 **“模式切换”** 来增强这种感知：

- **状态 1（调整中）**：UI 顶部显示 _“实时推演模式：基于数学公式调整分数”_。
- **状态 2（深度模拟后）**：UI 顶部显示 _“专家建模模式：基于最新参数重构因果逻辑”_。

---

### 5. 风险对冲：如何平衡一致性与真实性？

**这是最核心的取舍。** 如果每次深度模拟树都大变，用户会迷失。

**平衡方案：引入“逻辑基准（Logic Baseline）”**
我们可以借鉴代码版本控制（Git Diff）的思想：

- **保持骨架，更新节点**：要求 LLM 尽量保持树的深度和分支数量，但更新节点的 `name` 和 `logic_payload`。
- **高亮变动（The Delta）**：当新树替换旧树时，用一种极其克制的颜色（如浅蓝色背景）标记出**新产生的节点**。
- **文案解释分歧**：在 `recommendation.analysis` 中，强制要求 LLM 包含一段：
  > “对比初始模型：由于您将「开发效率」置于首位，系统剔除了原有的「生态长期维护」节点，转而预测了「快速交付后的技术债重构」路径。”

---

### 💡 深度总结：树为什么必须变？

你的 React 例子非常精彩。在“效率至上”的环境下，**“生态完善”确实不再是主要矛盾**，它在决策树上应该被缩减，甚至被“代码重用率”或“脚手架成熟度”取代。

**这种“树的变化”才是真正的高级。** 它告诉用户：

> “换了环境，不仅是分数变了，连规则都变了。”

**操作建议：**

1.  **接口层面**：增加一个 `isDeepSimulation` 标志位。
2.  **Prompt 层面**：增加一个 `constraints` 字段，锁定 `options`。
3.  **UI 层面**：给深度模拟一个“生成中”的仪式感，但结果呈现要简洁。

这种**“动态因果重组”**，才是把“AI 决策”与“普通计算器”拉开代差的关键。你觉得，如果我们在树上用虚线标记出“已失效的旧路径”，会不会让这种对比更直观？
