# Decision Copilot 演示演讲稿

---

## Part 1：一句话定位

大家好，项目叫 Decision Copilot。

**输入一句话，AI 自动构建决策沙盘，做量化仿真、对抗审查，输出可解释结论。**

把"拍脑袋"变成"结构化推演"。

---

## Part 2：演示（约 5 分钟）

### 步骤 1：输入问题

打开首页，左侧输入面板。不用填表格，直接打字——

> "300 分高考，选 985 还是出国？"

点击提交。自然语言，零门槛。

---

### 步骤 2：看决策树

系统自动识别选项、维度、权重。完成后生成决策树——

不是给一个答案，而是**把整个推理过程画出来**。每个节点带概率标签，不确定性一目了然。

---

### 步骤 3：拖动滑块

每个人关注点不同。有人看重风险，有人看重回报。拖动滑块，分数和排名实时变化。

**这里不是简单的线性计算**——不同方案对同一维度的敏感度不同。我负责表达偏好，系统负责推演结果。拖动幅度大的时候，方案排名可能直接翻转。

---

### 步骤 4：点击节点溯源

点树上任意一个节点，右边展开推理链：

- 到达概率怎么算出来的
- 当前权重下的综合评分
- 哪个维度拉动分数最多
- 分数差异来自哪个参数

每一步都有数据支撑，不是黑盒。

---

### 步骤 5：多 Agent 流水线（可选）

点"运行流水线"，四个 Agent 串行工作：

1. **框架** — 识别关键变量
2. **建模** — 评估方案路径
3. **仿真** — 蒙特卡洛 5000 次模拟
4. **综合** — 生成最终报告

中间发现问题会自动回溯修正，不需要人工干预。

---

### 步骤 6：对抗审查

系统不光信 AI 自己的结论，还做了三层验证：

- **蒙特卡洛仿真** — 5000 次跑分布
- **敏感性分析** — 参数变了结论还稳不稳
- **DEVIL 对抗** — 主动攻击结论、找认知偏置

结论经得起攻击，才敢给你。

---

### 步骤 7：界面调整（快速带过）

左侧面板宽度、底部面板高度都能拖拽，适配不同屏幕。

---

### 总结四句话

- **产品价值**：闭环完整，从问题到结论一键走完
- **AI 原生**：LLM 建模 + 实时计算 + 压力测试，三者互补
- **创新深度**：多 Agent、蒙特卡洛、对抗审查——专业分析师工作自动化
- **可复用性**：换个问题直接用，适合所有多选项、高不确定性的决策场景

---

## Part 3：备用 Q&A

**Q1：和 Excel 加 AI 插件有什么区别？**

Excel 需要你提前知道选项、维度、权重——这本身就很难。我们是用 LLM 完成"从无到有"的建模，用户只需要微调偏好。LLM 建模型，公式做实时响应，量化引擎做压力测试，三者互补。

**Q2：AI 输出的结果可信吗？**

所以我们做了三层验证——蒙特卡洛仿真、敏感性分析、对抗审查。不只看 AI 的结论，还要攻击它、验证它。

**Q3：目标用户是谁？**

需要做关键决策的管理者、创业者、项目负责人。先从内部工具开始，积累案例形成模板库，用户直接复用。

**Q4：拖动滑块时，概率为什么不变化？**

概率是路径上各事件的条件概率乘积，由决策树结构决定。滑块改变的是"到达结果后的评分偏移"，不影响"结果发生的概率"。偏好影响"值不值得"，不影响"能不能发生"。

**Q5：树上的概率是拍脑袋的吗？**

LLM 根据场景描述生成，每个概率都有定性标签——高/中/低——对应明确的数值区间，用户可以直观判断合不合理。蒙特卡洛仿真会进一步校准。

**Q6：AI 建模建错了怎么办？**

两种方式：一是用户在参数面板手动调整权重，实时修正；二是流水线自检机制，发现关键偏差自动回溯。

**Q7：能处理多复杂的决策？**

当前适合 3-8 个选项、3-6 个维度的中等复杂度决策。太简单的二元选择不需要这个工具，太复杂的几十个变量的我们还在优化上限。

**Q8：数据安全怎么保证？**

数据存在浏览器本地，不上传。后续如果要云端同步，会做加密存储。
