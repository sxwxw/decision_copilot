# 决策辅助产品：功能定位与交互路径讨论

## 一、产品背景

我们的产品是一个**决策辅助工具**，核心价值是帮助用户在面临两难选择时，通过定量建模 + 蒙特卡洛仿真 + 对抗性审查，得出更可靠的决策建议。产品差异化卖点是 **DEVIL 对抗性审查**——告诉用户"不要盲目相信 LLM 的结果"，让审查结果成为用户信任系统的最后一道防线。

---

## 二、当前系统的 3 个入口

当前左侧面板并列 3 个按钮：

### 1. 提交建模（/model 接口）
- 使用**精简版 prompt**（`DECISION_MODEL_PROMPT`）
- LLM 1 次调用，输出基础模型（选项、变量、权重、路径树）
- 速度快，但**不输出变量的概率分布（sim_spec）**，蒙特卡洛仿真回退到 uniform[0,100]

### 2. 多 Agent 流水线（/full-pipeline 接口）
- 4 个 Agent 串行调用：**FRAMEWORK → BUILD-MODEL → SIMULATE → DEVIL → NEXUS**
- BUILD-MODEL 阶段使用**完整版 prompt**（与深度模拟相同）
- 输出：决策框架 + 详细模型 + 对抗性审查 + 综合报告
- SSE 流式返回，前端有进度条
- **完全从头生成**，不参考页面上已存在的模型数据

### 3. 深度模拟（/simulate 接口）
- 使用**完整版 prompt**（`DECISION_MODEL_DEEP_PROMPT`）
- 输出带 sim_spec 的详细模型
- **唯一一个会参考当前模型上下文的操作**（基于已有模型 + 当前参数值重新推演）
- 完成后自动触发蒙特卡洛仿真 + DEVIL 审查

---

## 三、存在的问题

### 问题 1：3 个按钮让用户做选择题

用户看到 3 个并列按钮会困惑："我该点哪个？"
- 先点"提交建模"还是直接点"多 Agent"？
- "深度模拟"和"多 Agent"有什么区别？
- 点完一个之后要不要再点另一个？

**从用户心理模型看**，决策是一个渐进深入的过程：
```
我有一个问题 → 先看看大概 → 调调参数 → 深入验证 → 看看有没有漏洞
```
但当前 UI 把这变成了"3 条平行路径任选其一"，而非"一条路径逐步深入"。

### 问题 2：深度模拟和多 Agent 产出高度重叠

| 维度 | 深度模拟 | 多 Agent |
|------|---------|---------|
| 最终产出 | 详细模型 + 蒙特卡洛 + DEVIL 审查 | 详细模型 + 蒙特卡洛 + DEVIL 审查 + 综合报告 |
| 建模 prompt | `DECISION_MODEL_DEEP_PROMPT` | 同左（BUILD-MODEL 阶段） |
| 差别 | 缺少 FRAMEWORK 和 NEXUS | 多 2 个 Agent 步骤 |

用户几乎感觉不到两者的差异，但系统维护了两套几乎相同的逻辑。

### 问题 3：多 Agent 不参考当前模型，是"另起炉灶"

多 Agent 流水线只接收 `userInput`（用户原始问题），从头开始 5 步生成，**完全无视页面上已有的模型**。这导致：
- 用户先点"提交建模"生成一版 → 调整参数 → 再点"多 Agent"
- 流水线会生成一个**完全不同的新模型**（变量不同、分数不同、路径不同）
- 用户的参数调整白费了，前面的操作没有积累价值

### 问题 4：精简版 prompt 缺失 sim_spec

快速建模不要求 LLM 输出变量的概率分布（sim_spec），导致蒙特卡洛仿真对所有变量回退到 `uniform[0,100]`。DEVIL 审查会据此发出"变量缺少概率分布定义"的指控，影响用户对系统的信任。

> **已修复**：已在精简版 prompt 中补充 sim_spec 要求 + 变量命名方向性约束 + 前端 sanitizeModel 不再丢弃 sim_spec。

---

## 四、两个改造方向讨论

### 方向 A：保留 3 个按钮，优化交互逻辑

保持现有 3 个入口不变，但通过 UI 文案、按钮状态、操作引导来减少用户困惑。

**优点**：改动最小，不影响现有功能
**缺点**：根本问题（3 条平行路径）没有解决，用户仍然需要理解 3 种模式的区别

### 方向 B：重组为渐进式路径（推荐）

将 3 个按钮重组为 **2 个按钮 + 渐进式流程**：

```
阶段 1: 快速建模（原"提交建模"）
   ↓ 建模完成
阶段 2: 参数调整（拖滑块，实时看分数变化）
   ↓ 参数调整满意后
阶段 3: 多 Agent 深度验证（改造为基于当前模型的深入推演）
   ↓ 自动触发
阶段 4: 蒙特卡洛仿真 + DEVIL 审查（当前已有）
```

**具体改造点**：

1. **去掉"深度模拟"按钮** — 其功能（带 sim_spec 的深度建模）并入多 Agent 流水线的 BUILD-MODEL 阶段
2. **多 Agent 改为基于当前模型** — 接收当前模型的 options、variables、weights、treeData 作为上下文，在此基础上做增强推演（补充 sim_spec、推演新路径、深度审查），而非另起炉灶
3. **多 Agent 入口降级** — 从左侧主按钮区移到参数面板下方，文案改为"深度验证"或"完整分析"，暗示这是对当前模型的进一步操作
4. **没有模型时的处理** — 如果用户未建模就点"深度验证"，自动先执行快速建模，再继续深度流程

**优点**：
- 用户路径清晰：建模 → 调参 → 验证，一条线走到底
- 按钮从 3 个减到 2 个，降低认知负担
- 用户的每次操作都有积累价值（参数调整不会被多 Agent 覆盖）
- 消除了深度模拟和多 Agent 的功能重叠

**缺点/风险**：
- 多 Agent 改为基于当前模型后，BUILD-MODEL 阶段的 prompt 需要改造（改为 refine 模式？）
- 如果当前模型质量很差（LLM 第一次输出不理想），基于它做深度推演可能放大错误
- 需要确认：用户是否需要"重新来过"的能力？（比如对当前模型不满意，想从框架重新开始）

---

## 五、需要决策的问题

1. **是否接受方向 B？** 即去掉"深度模拟"按钮，将多 Agent 改为基于当前模型的深度验证？
2. **多 Agent 的 BUILD-MODEL 阶段如何处理？**
   - 选项 A：保持原样，生成全新模型（当前行为）
   - 选项 B：改为对已有模型的增强（基于 `DECISION_REFINE_PROMPT`，保持 options 和 weights 不变，补充 sim_spec、推演新路径）
   - 选项 C：混合模式——先跑 FRAMEWORK 定义框架，再用当前模型作为 BUILD-MODEL 的参考基准
3. **是否需要保留"重新来过"的能力？** 如果用户不满意当前模型，是清空重填问题，还是有一个"从框架重新开始"的按钮？
4. **精简版 prompt 加 sim_spec 后响应变慢，是否可以接受？** （增加了 LLM 的推理负担和 token 消耗）
