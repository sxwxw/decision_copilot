<!--
 * @Author: wxw
 * @Date: 2026-04-23 20:57:27
 * @LastEditors: wxw
 * @LastEditTime: 2026-04-23 20:57:28
 * @FilePath: \decision_copilot\方案三.md
-->

要实现“简洁大气、去 AI 味”的设计，核心在于**摒弃过度的渐变、霓虹发光和深色科幻背景**。我们需要向 **Linear、Apple 开发者文档或 Stripe** 的设计风格靠拢：采用大量的留白（Negative Space）、精致的灰色线条、清晰的层级以及克制的品牌色点缀。

````markdown
# ⚪ 极致简洁版 UI 视觉系统规范 (Minimalist Logic)

## 1. 设计哲学

- **白空间 (White Space)**：通过大量留白让数据“呼吸”，而非用颜色填满。
- **克制色彩**：背景纯白，仅在关键交互点（按钮、活动路径）使用单一品牌色。
- **细腻纹理**：用极细的边框（1px）取代阴影，用灰度等级区分层级。

---

## 2. 核心色调 (The Neutral Palette)

### 2.1 基础环境

- **全局背景 (Global BG)**: `#FFFFFF` (纯白)
- **容器背景 (Panel BG)**: `#F9FAFB` (极浅灰，用于侧边栏或次要区域)
- **边框线 (Border)**: `#E5E7EB` (丝滑的浅灰线，定义空间感)
- **文字主色 (Text Primary)**: `#111827` (接近黑色的深灰，保证阅读清晰度)
- **文字次色 (Text Secondary)**: `#6B7280` (中灰色，用于辅助说明)

### 2.2 功能点缀色 (Accent)

- **品牌逻辑色 (Action)**: `#3B82F6` (克制的明亮蓝，仅用于按钮和核心节点)
- **正向状态**: `#059669` (森林绿)
- **预警状态**: `#DC2626` (深红色)

---

## 3. 决策树视觉逻辑 (Non-AI Visuals)

为了去掉“AI味”，我们要移除所有发光和粒子效果：

- **节点 (Node)**:
  - 矩形框，直角或 4px 小圆角。
  - 白色底 + 1px `#E5E7EB` 边框。
  - 激活态：边框变为品牌蓝 `#3B82F6`，背景变为极浅蓝 `#EFF6FF`。
- **连线 (Path)**:
  - 颜色：`#D1D5DB` (浅灰色)。
  - 概率表现：通过线宽（1px - 3px）变化，不使用发光效果。
  - 激活路径：连线变蓝，保持实线。

---

## 4. 全局组件样式

### 4.1 滑块 (Slider)

- **轨道**: 极细的灰色横线。
- **滑块头 (Thumb)**: 白色圆环 + 灰色投影，极致简约。

### 4.2 推荐卡片 (Rank Card)

- 放弃大面积色彩填充，采用 **“列表式布局”**。
- 用数字 1, 2, 3 的粗体字号来体现排名，而不是用金银铜牌。

---

## 5. CSS 变量实现 (Minimalist Config)

```css
:root {
  --bg-main: #ffffff;
  --bg-secondary: #f9fafb;
  --text-main: #111827;
  --text-muted: #6b7280;
  --border-color: #e5e7eb;
  --brand-blue: #3b82f6;
  --brand-green: #059669;
  --node-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
}

/* 简洁节点样式 */
.decision-node {
  background: var(--bg-main);
  border: 1px solid var(--border-color);
  box-shadow: var(--node-shadow);
  color: var(--text-main);
  transition: all 0.2s ease;
}

.decision-node:hover {
  border-color: var(--brand-blue);
  transform: translateY(-1px);
}
```
````

---

## 6. 视觉逻辑小贴士

1.  **字体选择**：优先使用系统默认字体（San Francisco, Inter 或 Segoe UI），字重建议选择 400 (Regular) 和 600 (Semibold)。
2.  **图标风格**：使用 **Outline (描边型)** 风格的图标（如 Heroicons），线条粗细保持一致，不要使用双色或填充型图标。
3.  **对齐**：严格遵守栅格系统，所有卡片和面板的间距保持 8px 的倍数。

```

这种设计会让系统看起来像一个**严谨的生产力工具（SaaS 产品）**，而不是一个临时的 AI 演示页面。它能给用户一种“我在掌控工具”，而不是“工具在炫技”的踏实感。
```
