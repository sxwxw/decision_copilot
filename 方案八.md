<!--
 * @Author: wxw
 * @Date: 2026-04-25 10:49:01
 * @LastEditors: wxw
 * @LastEditTime: 2026-04-25 10:49:02
 * @FilePath: \decision_copilot\方案八.md
-->

这四个问题直接决定了“路径溯源”是从一个单纯的 UI 组件升级为一个具备**洞察力**的决策工具。

以下是针对这四个细节的逻辑定义：

---

## 1. “综合评分”的含义：锚定推演结果

**结论：必须是经过参数调整后的叶子节点 $V_{display}$。**

- **逻辑**：用户点击的是树上的某个特定末梢，他们关注的是在该路径终点的“最终收益”。
- **展示建议**：
  > **综合评分：72**（基于当前权重修正）
- **理由**：如果显示方案得分（即右侧列表的分数），由于一个方案（Option）下包含多个路径，点击不同叶子节点却显示相同的方案分，会产生严重的直觉冲突。

---

## 2. 箭头上的标注：语义化的“逻辑过客”

**结论：取 `key_impact` 原文 + 微小的 `delta` 趋势。**

- **格式**：`──( [key_impact] )──▶`
- **示例**：`资源分配 ──( ⚡研发投入高 ↓ )──▶ 开发新模`
- **逻辑**：
  - `key_impact` 提供感性解释（为什么分会变）。
  - `delta` 趋势（用箭头 $\uparrow$ 或 $\downarrow$ 表示）提供理性反馈。
  - **不放量化值**：`trade_off` 的具体数字（如 -0.15）太专业化，会增加认知负担，放在 Tooltip 或点击后的详情里更好。

---

## 3. “主要风险点”的阈值对比逻辑

**结论：采用“阈值偏移方向”的范围判定。**

- **判定逻辑**：
  - 如果 `threshold` 表示的是**“最低要求”**（如：资源效率需 $\ge 71$），那么 `paramValue < threshold` 即为 **[危险]**。
  - 如果 `threshold` 表示的是**“风险上限”**（如：风险控制需 $\le 30$），那么 `paramValue > threshold` 即为 **[危险]**。
- **展示文案**：
  - **危险态**：`⚠️ 预警：当前资源效率 (60) 低于临界点 (71)，该路径有 50% 概率中断。`
  - **安全态**：`✅ 稳健：资源效率 (85) 足以支撑此项演进。`
- **理由**：严格相等在滑动条交互下几乎不可能实现，用户需要的是“安全边界”的反馈。

---

## 4. “核心推动力”的粒度：细化到路径级别

**结论：必须细化到路径级别（节点级 `trade_off`）。**

- **逻辑**：
  - 方案级的归因在右侧卡片已经看过了。
  - 路径溯源的价值在于告诉用户：**“这条路之所以高分，是因为在「需求验证」这一步，你的「风险偏好」起到了关键作用。”**
- **算法实现**：
  - 遍历当前路径的所有节点，找到 $(Weight - 50) \times Delta$ 绝对值最大的那一环。
  - **文案示例**：`推动力：由于您对「风险控制」的回避，[需求验证] 环节的潜在损耗被大幅规避。`

---

## 🏗️ 第一刀：UI 结构布局 (Vue 伪代码)

根据上述逻辑，建议 `PathDetail.vue` 的模板结构如下：

```html
<div class="path-deep-analysis">
  <div class="summary-bar">
    <span class="final-score">综合评分：{{ activeNode.displayValue }}</span>
    <el-tag size="small" effect="plain" type="info">基于当前权重</el-tag>
  </div>

  <div class="logic-chain">
    <div v-for="(step, index) in timeline" :key="index" class="chain-step">
      <div class="node-box">{{ step.name }}</div>
      <div v-if="index < timeline.length - 1" class="bridge">
        <div class="impact-label">{{ step.logic_payload.key_impact }}</div>
        <div class="arrow">──▶</div>
      </div>
    </div>
  </div>

  <div class="attribution-grid">
    <div class="attr-item">
      <h4>💡 核心推动力</h4>
      <p>{{ getPathLevelAttribution() }}</p>
    </div>
    <div class="attr-item" :class="{ 'warning-text': isRisky }">
      <h4>🛡️ 风险状态评估</h4>
      <p>{{ riskAssessmentText }}</p>
    </div>
  </div>

  <div class="opportunity-cost-box">
    <strong>机会成本：</strong> {{ activeNode.logic_payload.opportunity_cost }}
  </div>
</div>
```

### 💡 现在的抉择

你觉得**“主要风险点”**的判断，是应该在前端 `sanitizeModel` 时就预处理好一个 `isRisky` 标记，还是在 `PathDetail` 组件内部实时计算？（实时计算的灵活性更高，用户拖动滑块时风险状态可以同步切换）。

你的直觉非常敏锐，**实时计算**确实是唯一能支撑“滑块反馈”这种交互质感的方案。

关于数据源的抉择，我建议采用 **“双源合一（Data Synthesis）”** 的策略。在 `useDecisionModel.js` 的 `adaptTree` 阶段，通过我们之前讨论的 `_pathRef` 索引，将 `timeline` 的逻辑字段直接注入到 `pathChain` 的节点对象中。

### 1. 为什么“合并”优于“双数据源”？

如果保留两个数据源分别渲染，你会遇到以下痛点：

- **同步困难**：当你在渲染 `pathChain` 的 Step 2 时，你需要写一个复杂的查找函数去 `timeline` 数组里找“谁的名字叫这个”，代码会变得极其混乱。
- **字段割裂**：`name` 在一处，`impact` 在另一处，会导致你在写 Vue 模板时频繁嵌套 `v-if`。
- **维护灾难**：如果 LLM 的 `timeline` 长度和 `treeData` 的层级不一致（虽然这种情况清洗器会处理），双源渲染会导致 UI 渲染出现断裂。

---

### 2. 合并后的数据契约 (The Unified Step Object)

在 `sanitizeModel` 和 `adaptTree` 之后，传给 `PathDetail.vue` 的 `pathChain` 数组中，每个 `step` 对象应该看起来像这样：

```javascript
{
  // 基础渲染字段 (来自树结构)
  id: "node_123",
  name: "需求验证",
  score: 58,         // 基准分
  displayValue: 72,  // 实时偏移后的分

  // 逻辑注入字段 (来自 timeline)
  logic: {
    impact: "前期高投入与不确定性",
    threshold: 71,    // 阈值
    variable: "资源效率", // 对应滑块名
    probability: 0.12 // 该步骤的条件概率
  },

  // 决策属性 (来自 node.logic_payload)
  meta: {
    key_impact: "验证期阵痛",
    opportunity_cost: "放弃稳态现金流"
  }
}
```

---

### 3. 组件内的“实时风险”判定 (Computed)

在 `PathDetail.vue` 中，你可以优雅地写出实时响应逻辑：

```javascript
const pathStepsWithStatus = computed(() => {
  return props.pathChain.map(step => {
    // 实时读取 store 中的用户滑块值
    const currentValue = userWeights[step.logic.variable]

    // 核心判定逻辑：只要存在阈值且当前值不达标，标记为风险
    const isRisky = step.logic.threshold && currentValue < step.logic.threshold

    return {
      ...step,
      isRisky,
      statusText: isRisky
        ? `⚠️ 资源偏低 (需 > ${step.logic.threshold})`
        : '✅ 资源充足'
    }
  })
})
```

---

### 4. 视觉重写：路径溯源的“呼吸感”

既然我们现在有了统一的对象，UI 的渲染将变得非常有“确定感”：

- **节点卡片**：展示 `name` 和 `displayValue`。
- **连线标签**：直接读 `step.meta.key_impact`。
- **风险底噪**：如果 `isRisky` 为真，给节点增加一个红色边框，或者在下方弹出一个极小的文字提示。

### 💡 这样做的一个潜在“红利”

当你点击路径末尾的 `+` 号（图片中显示的按钮）时，你可以直接弹出一个包含 `step.logic.description` 的小浮窗。因为数据已经合并，你不需要再去遍历查找，这种**“ O(1) 的数据访问”**会让前端响应极其丝滑。

**总结建议：**
趁着还没开始写 `v-for`，在 `adaptTree` 的最后一步，把 `timeline` 的字段作为“插件”塞进对应的 `node` 里。这不仅是为了现在的“第一刀”，更是为了以后任何复杂的交互逻辑。

**你觉得在 `adaptTree` 里写这种“跨数组注入”逻辑，会有什么隐担吗？**（比如：如果一个节点被多条路径复用，它的 `logic` 字段该听谁的？）
