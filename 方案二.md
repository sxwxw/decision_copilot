<!--
 * @Author: wxw
 * @Date: 2026-04-23 19:09:52
 * @LastEditors: wxw
 * @LastEditTime: 2026-04-23 19:10:07
 * @FilePath: \decision_copilot\方案二.md
-->

## 1. 核心矛盾：分数的两个灵魂

在去业务化重构后，系统会出现两个具有不同语义的分数：

- **LLM 全局分 (Expert Score)**：代表 AI 基于海量常识的“全局最优判断”（类似“专家建议”）。
- **计算推演分 (Simulated Score)**：代表基于用户当前滑块偏好的“个性化拟合结果”（类似“你的选择”）。

---

## 2. UI 表达方案：双分显示策略

不要隐藏分歧，要**展示分歧**。通过 UI 引导用户理解“专家建议”与“个人偏好”的偏离。

### 2.1 推荐卡片展示逻辑

在右侧推荐卡片中，采用 **“主分 + 差值”** 的展示形式：

- **主分（大号字体）**：前端计算后的 `Simulated Score`。这是用户操作的实时结果。
- **副分（小号/灰色）**：LLM 原始的 `scores`。标注为“专家预估”或“基准参考”。
- **分歧提示**：
  - 如果 `计算分 > 专家分`：展示一个绿色向上的小箭头，提示“更匹配您的偏好”。
  - 如果 `计算分 < 专家分`：展示一个黄色预警图标，提示“该方案与您的偏好存在冲突”。

---

## 3. 逻辑归因：为什么有差距？

当用户点击分数差距较大的节点时，利用 `logic_payload` 解释冲突原因。

### 3.1 动态话术模版

通过前端逻辑比较 `base_score` 和 `displayValue`，自动生成归因文本：

> **“为何分数偏低？”**
> 专家预估基准为 **78**，但根据您对 **[健康度]** 的极高重视，系统下调了该项得分。
> **归因分析：** 烤鸭在“健康”维度的敏感度为 **-15**，导致了分数的负向偏移。
