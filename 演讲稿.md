# Decision Copilot 系统演示稿

---

## Part 1：30 秒定位

大家好，今天演示的项目叫 Decision Copilot。

它的核心能力是：**输入自然语言问题，AI 自动构建决策沙盘，量化仿真、对抗审查，输出可解释结论。**

把"拍脑袋 + Excel"重构为"结构化推演 + 量化验证"。

---

## Part 2：交互式 Demo（约 5 分钟）

### 步骤 1：打开首页，输入决策问题

> **操作**：打开系统首页，看到左侧输入面板。

"首先看到的是首页，左侧是输入面板。不需要填任何表格，只用自然语言描述场景——比如输入 '300 分高考，选 985 还是出国？'，点击提交。"

> **交互要点**：强调自然语言输入，零门槛建模。

---

### 步骤 2：等待建模，展示决策树生成

> **操作**：提交后展示 loading 状态，完成后展示决策树。

"系统正在调用 LLM 自动识别选项、评估维度、权重关系。建模完成后，生成这棵决策树——不是给一个答案，而是展示完整的推理结构。"

> **交互要点**：树节点带概率标签，展示 AI 对不确定性的量化。

---

### 步骤 3：拖动参数滑块，展示实时评分变化

> **操作**：在参数面板拖动滑块（如"风险控制"、"回报偏好"），观察决策树节点分数和排名变化。

"每个人的关注点不同。有人更看重风险控制，有人更在意回报。拖动滑块，分数和排名会实时变化。"

"这里不是简单的线性计算，而是差异化敏感度公式——不同方案对同一维度变化的响应不同。我负责表达偏好，系统负责推演结果。"

> **交互要点**：
> - 滑块拖动的即时反馈
> - 节点分数/颜色随参数变化
> - 方案排名可能翻转

---

### 步骤 4：点击树节点，展示路径溯源

> **操作**：点击决策树上某个中间节点或叶节点，右侧/下方面板展示路径详情。

"点击树上任意节点，查看完整推理链路：

- **到达总概率**：路径上各事件条件概率的累积
- **综合评分**：基于当前权重修正后的分数
- **路径链可视化**：Step-by-Step 展示推理过程
- **核心推动力**：哪个维度拉动分数变化最多
- **归因分析**：分数差异来自哪个参数的调整
"

> **交互要点**：面板信息密度高，突出"可解释"——每一步都有数据支撑。

---

### 步骤 5：触发多 Agent 流水线（可选）

> **操作**：点击"运行流水线"按钮，展示分阶段进度动画。

"对于重要决策，可以触发多 Agent 流水线——四个独立 Agent 分别做：

1. **框架构建** — 识别关键变量
2. **建模** — 评估各方案路径
3. **仿真** — 蒙特卡洛 5000 次模拟
4. **综合分析** — 生成 Nexus 报告

每一步互不干扰，降低系统性偏差。流水线还有自我校准机制——发现偏差时自动回溯修复。"

> **交互要点**：打字机效果的阶段提示，流水线进度可视化。

---

### 步骤 6：查看对抗审查与敏感性分析

> **操作**：在综合报告的 NEXUS 标签页，展示 DEVIL 对抗结果和敏感性分析。

"系统不只看 AI 的结论，还做了三层验证：

- **蒙特卡洛仿真** — 量化不确定性分布
- **敏感性分析** — 检验结论对参数变化的稳定性
- **DEVIL 对抗审查** — 主动攻击结论、检测认知偏置

让结论不再是黑盒。"

> **交互要点**：展示对抗审查的具体攻击点和偏差检测结果。

---

### 步骤 7：拖拽面板，展示布局灵活性

> **操作**：拖动左侧面板宽度分隔线、底部面板高度分隔线。

"界面支持自由调整——左侧面板可以拉宽查看更多参数，底部面板可以拉高查看完整报告。适应不同屏幕和使用习惯。"

---

### 评分维度对照总结

- **产品价值**：解决决策缺乏系统推演的痛点，闭环完整
- **AI 原生程度**：LLM 建模 + 前端实时响应 + 量化引擎压力测试，三者互补
- **极限创新能力**：多 Agent 推演、蒙特卡洛仿真、对抗审查——将专业分析师工作自动化
- **用户与运营价值**：换一个问题直接复用，适用于所有多选项、多变量、高不确定性场景

---

## Part 3：评委交流（备用 Q&A）

### 可能被问到的问题与回答：

**Q1：这和用 Excel 加 AI 插件有什么区别？**

Excel 的决策矩阵需要你预先知道有哪些选项、哪些维度、怎么分配权重——这本身就需要决策能力。我们是用 LLM 完成建模最难的前置工作，用户只需在 AI 生成的模型上微调偏好。LLM 负责"从无到有"，前端公式负责"实时响应"，量化引擎负责"压力测试"，三者互补。

**Q2：AI 输出的结果可信吗？**

这正是系统设计的核心考量。我们不只看 AI 的结论，还做了三件事：一是蒙特卡洛 5000 次仿真量化不确定性；二是敏感性分析检验结论的稳定性；三是 DEVIL 对抗审查主动攻击结论、检测认知偏置。这三层验证让结论不再是黑盒。

**Q3：你们的用户是谁？怎么推广？**

目标用户是需要做关键决策的管理者、创业者和项目负责人。推广路径可以从内部工具开始，积累典型案例后形成模板库，用户可以直接复用已有案例的结构。

**Q4：拖动参数滑块时，概率为什么不变化？**

到达总概率是路径上各事件条件概率的累积乘积，由决策树的结构和节点固有概率决定。参数滑块改变的是"到达某个结果后的评分偏移"，而不是"到达这个结果的概率"。这是有意设计——偏好影响"值不值得"，不影响"能不能发生"。如果后续需要参数影响概率，可以建立参数到条件概率的映射层。

**Q5：决策树的概率是怎么来的？拍脑袋吗？**

概率值由 LLM 在建模阶段根据用户描述的场景自动生成，基于大语言模型对世界知识的概率直觉。每个概率都有定性标签（高/中/低）对应定量区间，用户可以直观理解。后续可以通过蒙特卡洛模拟进一步校准。

**Q6：如果 AI 建模建错了怎么办？**

系统提供了两种纠错机制：一是用户可以在参数面板手动调整权重和偏好，实时修正模型输出；二是多 Agent 流水线的自我校准机制——当检测到关键偏差时会自动回溯修复模型，用户也可以手动触发模型修正。

**Q7：这个系统能处理多复杂的决策？**

当前版本适合 3-8 个选项、3-6 个评估维度的中等复杂度决策。过于简单的二元选择不需要这个工具，过于复杂的战略决策（几十个变量）可能需要更专业的建模。我们正在优化上限，让系统能处理更大规模的决策树。

**Q8：数据安全怎么保证？**

当前版本数据存储在浏览器本地（localStorage），不会上传到服务器。用户的决策问题和模型数据完全私有。未来如果要支持云端同步，会采用加密存储和权限控制。
